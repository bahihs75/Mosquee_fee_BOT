# التقرير النهائي: بوت استرجاع مصاريف المهام

## 1. النتيجة

تم إنشاء مشروع مستقل لبوت تيليغرام خاص لمعالجة طلبات استرجاع المصاريف عبر دورة واضحة من ثلاثة أطراف: **المستخدم الأول** الذي دفع المصروف، **مجموعة الإدارة** التي تراجع الطلب وتعدله أو تلغيه أو تعتمده، و**الصندوق** الذي يؤكد دفع المبلغ فعلياً.

المشروع لا ينسخ منطق البوت القديم، بل يعيد استخدام الفكرة العامة فقط ويضع منطقاً مستقلاً يعتمد على الحالات الصريحة، النسخ المتعددة للطلب، سجل التدقيق، وقفل قاعدة البيانات أثناء الانتقالات المالية.

## 2. مسار العمل المعتمد

| المرحلة | المنفذ | التفاصيل |
|---|---|---|
| التسجيل | المستخدم الأول | يرسل اسمه الرسمي. يُحفظ الاسم مع `telegram_user_id` واسم المستخدم ورابط ملف تيليغرام. لا يستطيع المستخدم تغيير الاسم لاحقاً. |
| إنشاء الطلب | المستخدم الأول | يدخل اسم المسجد أو المهمة، الولاية، مدة المهمة، المبلغ بالدينار الجزائري، والملاحظات الإضافية. يمكن إضافة صورة أو ملف، ويمكن لاحقاً جعل المرفق إلزامياً من إعداد قاعدة البيانات. |
| مراجعة الإدارة | أي عضو في المجموعة | الأعضاء متساوون في الصلاحيات. يستطيع أي عضو اعتماد الطلب، طلب تعديل من المستخدم، تعديل البيانات مباشرة، أو إلغاء الطلب بسبب إلزامي. يسجل النظام اسم ومعرف المنفذ. |
| إعادة الإرسال | المستخدم الأول | إذا طلبت الإدارة تعديلاً، ينشئ المستخدم نسخة جديدة من الطلب دون حذف النسخة السابقة. |
| الصندوق | المستخدم أو المجموعة المضبوطة للصندوق | يرى الطلب بعد اعتماد الإدارة فقط. يستطيع بدء المراجعة، تأكيد الدفع الكامل، تسجيل دفع جزئي إذا فُعّل، أو رفض/تعليق الدفع بسبب إلزامي. |
| الإغلاق | النظام | عند الدفع الكامل تصبح الحالة `paid_confirmed`، ويُبلّغ المستخدم والإدارة، ويدخل الطلب في ملف Excel. عند الإلغاء أو الرفض يُبلّغ الطرف المعني ولا يدخل الطلب في ملف المؤكدين. |

> **القاعدة المالية الأساسية:** لا يعني اعتماد الإدارة أن المستخدم استلم المال؛ الاعتماد يحوّل الطلب إلى الصندوق، أما الإغلاق المالي فلا يحدث إلا بعد تأكيد الصندوق.

## 3. الحالات التي يغطيها المنطق

| الحالة | ما يحدث |
|---|---|
| مستخدم جديد | يمر عبر تسجيل الاسم قبل إنشاء أي طلب. |
| محاولة تغيير الاسم من المستخدم | لا تُقبل؛ التغيير متاح من مجموعة الإدارة فقط. |
| وجود أكثر من طلب | لكل طلب رقم دائم ونسخ مستقلة وسجل أحداث مستقل. |
| طلب تعديل من الإدارة | يصل للمستخدم مع السبب وزر التعديل وإعادة الإرسال. |
| تعديل مباشر من الإدارة | يحفظ نسخة جديدة ويطلب اعتماد النسخة المعدلة قبل إرسالها للصندوق. |
| إلغاء الإدارة | يلزم سبب، ويُبلّغ المستخدم. |
| إلغاء المستخدم | مسموح فقط في الحالات التي لم تُغلق، وتُبلّغ الإدارة. |
| اعتماد مزدوج من عضوين | يسمح قفل PostgreSQL بنجاح عملية واحدة فقط؛ العملية الثانية تفشل برسالة واضحة. |
| ضغط زر قديم بعد تعديل الطلب | يفحص النظام الحالة ورقم النسخة، فلا يسمح بالاعتماد أو الدفع على نسخة قديمة. |
| رفض أو تعليق الصندوق | يلزم سبب، ويُبلّغ المستخدم والإدارة، ويبقى الطلب خارج قائمة المؤكدين. |
| دفع جزئي | معطل افتراضياً. إذا فُعّل، يحسب النظام مجموع الدفعات التراكمية ولا يغلق الدورة حتى يكتمل المبلغ. لا يسمح بتجاوز المبلغ المعتمد. |
| فشل رسالة خاصة | يُحفظ الحدث المالي أولاً، وتُحفظ محاولة الإرسال وحالتها والخطأ لإعادة المحاولة. يمكن للمستخدم رؤية الحالة من قائمة طلباته بعد فتح البوت. |
| فشل رسالة مجموعة أو صندوق | لا يُلغى التغيير المالي؛ يبقى في سجل الإشعارات الفاشلة مع محاولة دورية لاحقة. |
| عدم وجود وجهة للصندوق | لا يضيع الطلب؛ يبقى معتمداً بانتظار ضبط وجهة الصندوق من الإدارة. |
| توقف البوت أو إعادة تشغيله | البيانات الدائمة في PostgreSQL، بينما جلسة إدخال الحقول الحالية مؤقتة. لذلك إذا توقف البوت أثناء تعبئة الحقول، يبدأ المستخدم الإدخال من جديد، أما الطلبات المحفوظة فلا تضيع. |
| تصدير Excel | يصدّر النظام الطلبات ذات حالة الدفع الكامل فقط، مع اسم المستخدم، رابط ملفه، المهمة، الولاية، المدة، المبالغ، طريقة الدفع، ملاحظات الدفع، منفذ التأكيد، وتاريخ التأكيد. |

## 4. حفظ المستخدمين وقائمة الإدارة

جدول `users` هو المصدر الدائم لقائمة المستخدمين. يحفظ الاسم الرسمي، معرف تيليغرام، اسم المستخدم الحالي، رابط الملف، الحالة، تاريخ التسجيل، وآخر ظهور. ويمكن لأعضاء مجموعة الإدارة استخدام `/users` لعرض القائمة وتغيير الاسم بواسطة زر مخصص. حتى إذا لم يكن لدى المستخدم أي طلب، يسجل تغيير اسمه في `workflow_events` عبر `target_user_id`.

رابط الملف قد يكون رابط `https://t.me/username` إذا كان للمستخدم اسم مستخدم عام، أو رابط `tg://user?id=...` للمستخدم الذي لا يملك اسماً عاماً. يجب اعتبار الرابط الثاني رابطاً داخلياً داخل تيليغرام وليس رابطاً عاماً للويب.

## 5. ملاحظات مهمة حول مجموعة الإدارة

بما أن جميع أعضاء المجموعة متساوون، فإن كل عضو موجود في `ADMIN_GROUP_ID` يستطيع تنفيذ إجراءات الإدارة. لذلك يجب أن تكون المجموعة خاصة ومضبوطة العضوية؛ فالمشروع لا يميز بين مدير ومدير آخر داخل هذه المجموعة. هذا يطابق قرارك الحالي، لكنه يعني أن إضافة شخص إلى المجموعة تمنحه صلاحيات الاعتماد والإلغاء وتعديل الأسماء والتصدير.

إذا كان Privacy Mode مفعلاً للبوت، فيجب على أعضاء الإدارة والصندوق إرسال الأسباب وبيانات الدفع **كرد Reply على رسالة البوت**، أو يجب تعطيل Privacy Mode للبوت من BotFather حتى تصل الرسائل النصية العادية في المجموعة. هذه نقطة تشغيلية مهمة لأن الأزرار وحدها لا تكفي لاستلام سبب الإلغاء أو بيانات الدفع.

## 6. الحالات غير المنطقية أو الثغرات التي لاحظتها وصُححت

أولاً، كان ربط تعديل اسم المستخدم بآخر طلب له منطقياً غير صحيح؛ المستخدم قد لا يملك أي طلب، أو يملك عدة طلبات، وتغيير الاسم يخص المستخدم لا طلباً عشوائياً. لذلك أصبح سجل التعديل مرتبطاً بـ`target_user_id` مع `request_id = NULL`.

ثانياً، الاعتماد على زر قديم أو ضغط عضوين في الوقت نفسه كان يمكن أن يؤدي إلى انتقالين متعارضين في أي نظام لا يفحص الإصدار. لذلك أضيف `version_no` وقفل `SELECT ... FOR UPDATE` وشروط الحالة والإصدار داخل التحديث الذري.

ثالثاً، الدفع الجزئي إذا حُسبت كل دفعة كأنها المبلغ النهائي قد يؤدي إلى اعتبار دورة غير مكتملة مدفوعة أو إلى السماح بتجاوز المبلغ. تم تصحيح ذلك ليحسب النظام `already_paid + new_payment`، ويغلق الدورة فقط عند مساواة المجموع للمبلغ المعتمد.

رابعاً، فشل إرسال إشعار تيليغرام لا يجب أن يلغي العملية المالية. لذلك جرى فصل سجل الحالة عن سجل الإشعار في `delivery_messages`، مع تخزين الخطأ وإعادة المحاولة الدورية.

خامساً، لا يستطيع البوت بدء محادثة خاصة مع مستخدم لم يرسل له `/start` من قبل. في هذا التصميم لا تظهر هذه المشكلة للمستخدم المسجل لأنه يبدأ المحادثة أثناء التسجيل، لكن يجب أخذها بعين الاعتبار إذا أضيفت لاحقاً وظيفة استيراد مستخدمين من قائمة خارجية.

سادساً، المرفقات اختيارية افتراضياً، والدفع الجزئي معطل افتراضياً. هذا قرار قابل للتغيير، وليس خطأً منطقياً، لكن ينبغي للإدارة تحديد السياسة قبل التشغيل الفعلي. بالنسبة للمصاريف المالية، أوصي بجعل صورة الإيصال إلزامية بعد تجربة أولية قصيرة.

## 7. الملفات التي تم إنشاؤها

| الملف | الوظيفة |
|---|---|
| `app.py` | التطبيق الرئيسي، التسجيل، الطلبات، مجموعة الإدارة، الصندوق، webhook/polling، الإشعارات والمهام الدورية. |
| `domain.py` | حالات المجال، الانتقالات، التحقق من الحقول والمبالغ، ومنع الانتقالات غير المسموحة. |
| `db.py` | PostgreSQL، المستخدمون، الطلبات، النسخ، سجل التدقيق، الدفع، الإشعارات، وقفل التزامن. |
| `schema.sql` | مخطط الجداول والفهارس والإعدادات الأولية. |
| `formatting.py` | الرسائل العربية وملخصات الطلبات وروابط المستخدمين. |
| `excel_export.py` | ملف Excel للطلبات المدفوعة بالكامل مع ورقة تعريف الحقول. |
| `backup.py` | نسخة JSON مضغوطة للجداول الدائمة، قابلة للتشغيل يدوياً أو من مهمة دورية. |
| `render.yaml` | إعداد نشر Render مع webhook وhealth check ومتغيرات البيئة. |
| `.env.example` | قائمة المتغيرات المطلوبة دون أسرار فعلية. |
| `README.md` | تعليمات التشغيل والأوامر والسياسات. |
| `tests/` | اختبارات الحالات والدفعات الجزئية وExcel والعقود الأساسية. |

## 8. نتائج الاختبار

تم اجتياز **18 اختباراً**، إضافة إلى فحص صياغة ملفات Python وفحص استيراد التطبيق الرئيسي بمتغيرات تجريبية. لم يُنفذ اختبار حي على Telegram أو PostgreSQL لأن توكن البوت وبيانات قاعدة البيانات الحقيقية لم تُقدّم في هذه المرحلة.

## 9. خطوات التشغيل قبل الإنتاج

1. إنشاء البوت من BotFather وتفعيل الإعداد المناسب لـPrivacy Mode.
2. إنشاء PostgreSQL وضبط `DATABASE_URL` و`ADMIN_GROUP_ID` و`BOT_TOKEN`.
3. إضافة البوت إلى مجموعة الإدارة ومنحه الصلاحيات اللازمة لقراءة الرسائل وإظهار الأزرار.
4. نشر المشروع على Render باستخدام `render.yaml`، ثم ضبط `RENDER_EXTERNAL_URL` و`WEBHOOK_SECRET_TOKEN`.
5. تشغيل `/start` من حساب المستخدم الأول، ثم تشغيل `/set_cashier user <telegram_id>` أو `/set_cashier group <chat_id>` من مجموعة الإدارة.
6. تنفيذ دورة تجريبية كاملة: تسجيل، طلب، طلب تعديل، إعادة إرسال، اعتماد، تأكيد صندوق، ثم `/export`.
7. اتخاذ قرار نهائي بشأن إلزام المرفق، السماح بالدفع الجزئي، ومدة اعتبار الطلب متأخراً.
8. جدولة تشغيل `backup.py` وحفظ الملفات الناتجة خارج نفس بيئة التطبيق إذا كانت سياسة الاستضافة قد تفقد القرص عند إعادة البناء.

## 10. مراجع تقنية

[1] [Telegram Bot API — Webhooks and bot methods](https://core.telegram.org/bots/api)

[2] [Render Web Services Documentation](https://render.com/docs/web-services)
